#ifndef UTIL_MATRICES_C
#define UTIL_MATRICES_C

#endif // UTIL_MATRICES_C

#include <util_matrices.h>

/*
#define COLS_MAX 1000
void rand_matrix(int rows, int cols, int A[][COLS_MAX], int min, int max)
{
    //You have to use COLS_MAX when using a matrix array
    for (int i = 0; i<rows; i++) //rows
    {
        for (int j = 0; i<cols; j++)        //columbs
        {
            A[i][j] = min + rand() %(max-min+1);
        }
    }
}
*/
